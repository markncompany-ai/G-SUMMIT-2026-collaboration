# GitHub Pages는 커밋된 파일을 그대로 공개 서빙한다.
# 화이트리스트 방식 — index.html 외에는 어떤 파일도 커밋되지 않는다.
# 새 파일을 배포하려면 아래에 예외(!)를 명시적으로 추가할 것.
/*
!/.gitignore
!/index.html

# 버전 고정 스냅샷 — 링크를 보낸 뒤에도 그 시점 문서를 열 수 있게 남긴다.
# 대외 공유가 승인된 버전만 추가할 것.
!/v3.html
!/v4.html
!/v5.html
